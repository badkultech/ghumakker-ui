export interface UserPreference {
    language: string;
    currency: string;
    timezone: string;
    dateFormat: string;
}

export interface UserPreferenceRequest {
    language: string;
    currency: string;
    timezone: string;
    dateFormat: string;
}
